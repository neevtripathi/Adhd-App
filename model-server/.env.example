# Port the model server listens on. Most deploy platforms inject this automatically.
PORT=9090

# Path to the model checkpoint. Defaults to ./checkpoints/model.pt if unset.
MODEL_CHECKPOINT=./checkpoints/model.pt
